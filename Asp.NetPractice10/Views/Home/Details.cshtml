@model User

@{
    ViewBag.Title = Model.Name;
}

<h2>User @Model.Name</h2>

<div class="form-group p-3"></div>

<div>
    <dl class="dl-horizontal">
        <dt>Identifier</dt>
        <dd>@Model.Id</dd>

        <dt>Age</dt>
        <dd>@Model.Age</dd>
    </dl>
</div>